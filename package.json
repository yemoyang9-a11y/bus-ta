{
  "name": "bus-ta",
  "version": "0.0.1",
  "private": true,
  "description": "AI·BLE 기반 시각장애인 대중교통 탑승·하차 보조 시스템 모노레포",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "typecheck": "pnpm -r --filter=!./apps/mobile exec tsc --noEmit",
    "build": "pnpm -r --filter=!./apps/mobile run build",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
