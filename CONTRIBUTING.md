# 기여 가이드

이 문서는 팀원이 같은 방식으로 브랜치를 만들고, 커밋하고, Pull Request를 작성하기 위한 규칙입니다.

## 브랜치 규칙

- `main`: 최종 제출 또는 배포 가능한 안정 버전입니다.
- `develop`: 팀 개발 내용을 통합하는 브랜치입니다.
- `feature/기능명`: 새로운 기능 개발 브랜치입니다.
- `fix/수정명`: 오류 수정 브랜치입니다.
- `docs/문서명`: 문서 수정 브랜치입니다.

브랜치 이름은 가능한 한 영어 소문자와 하이픈을 사용합니다.

```text
feature/voice-destination-input
fix/bis-arrival-error
docs/api-spec-update
```

## 작업 전 최신 코드 동기화

작업을 시작하기 전에 원격 저장소의 최신 코드를 가져옵니다.

```bash
git fetch origin
git pull origin 브랜치명
```

다른 사람이 같은 파일을 수정했을 수 있으므로, 작업 전과 Pull Request 전에는 항상 최신 상태를 확인합니다.

## 커밋 메시지 규칙

커밋 메시지는 다음 형식을 사용합니다.

```text
타입: 변경 내용 요약
```

예시:

- `feat: 새로운 기능 추가`
- `fix: 오류 수정`
- `docs: 문서 수정`
- `refactor: 코드 구조 개선`
- `test: 테스트 추가`
- `chore: 설정 및 기타 작업`

한 커밋에는 가능한 한 하나의 목적만 담습니다.

## Pull Request 작성 규칙

Pull Request에는 다음 내용을 작성합니다.

- 변경 목적
- 주요 변경 파일
- 구현 또는 수정 내용
- 실행한 테스트 또는 확인 명령어
- 테스트 결과
- 확인하지 못한 부분
- 관련 문서 수정 여부

API 요청 또는 응답 구조가 바뀐 경우 `docs/API_SPEC.md` 수정 여부를 반드시 표시합니다.

## 코드 리뷰 규칙

- 리뷰어는 동작 오류, API 불일치, 보안 문제, 테스트 누락을 우선 확인합니다.
- 작성자는 리뷰 의견에 대해 수정 여부와 이유를 댓글로 남깁니다.
- 실행하지 않은 테스트를 통과했다고 작성하지 않습니다.
- 단순 취향보다 프로젝트 문서와 기존 코드 규칙을 우선합니다.

## 충돌 발생 시 처리 방법

1. 충돌 파일을 열어 현재 브랜치와 대상 브랜치의 차이를 확인합니다.
2. 임의로 한쪽 내용을 삭제하지 않습니다.
3. API 명세나 DB 스키마가 충돌하면 관련 팀원에게 확인합니다.
4. 충돌 해결 후 관련 기능을 다시 실행하거나 문서 링크를 확인합니다.
5. 어떤 충돌을 어떻게 해결했는지 Pull Request에 기록합니다.

## API 변경 공유 규칙

API 주소, 요청 Body, 응답 Body, 상태 코드, 필드 이름이 바뀌면 반드시 팀원에게 공유합니다.

- `docs/API_SPEC.md`를 함께 수정합니다.
- 프론트엔드와 백엔드 담당자에게 변경 내용을 알립니다.
- 기존 필드를 제거하거나 이름을 바꾸는 경우 영향 범위를 확인합니다.
- 확정되지 않은 변경은 `초안` 또는 `확인 필요`로 표시합니다.
